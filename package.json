{
  "name": "prettier-dollar-sign-bug",
  "version": "1.0.0",
  "description": "A repo to validate a bug in prettier where it incorrectly cannot locate files starting with a '$' character.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
